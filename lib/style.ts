import { Button } from "react-native";
import { styled } from "tamagui";

export const OrangeButton = styled(Button, {
  background: '$orange9',
  color: 'white',
  hoverStyle: { background: '$orange10' },
  pressStyle: { background: '$orange8' },
})
